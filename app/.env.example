# Optional: custom RPC endpoint for the app (recommended on mainnet — the public
# mainnet RPC is heavily rate-limited). Copy to `.env` and set your endpoint.
# Get one from Helius, Triton, QuickNode, etc.
VITE_RPC_URL=https://your-rpc-endpoint.example.com
